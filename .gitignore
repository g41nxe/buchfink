# runtime data (ADR 12)
data/
# Das Vokabular der Merkmale und Erzählmuster beruht auf NoveList (EBSCO); ob
# es veröffentlicht werden darf, ist ungeklärt (#59). Bis dahin liegt es nur
# lokal. Ohne es gibt es keinen Steckbrief und keine Erstaufnahme.
vocabulary/
# Sicherungen der Datenbank aus dem Container (docs/betrieb.md)
data-sicherung/

# Umgebung des Containers — traegt Schluessel und Token. Alles, was so
# heisst: eine `.env.bak` neben der `.env` traegt dasselbe Geheimnis und war
# von `.env` allein nicht gedeckt. Die Vorlage ist die eine Ausnahme.
.env*
!.env.example

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# skill scratch / working tickets
.scratch/

# Ad-hoc-Testlaeufe gegen die echten Quellen
testlauf/

# Prototypen-Server (zeigt auf .scratch/)
.claude/launch.json

# Rohdaten der Metadaten-Recherche
metadata-probe.json

# Bauergebnis der Oberflaeche (ADR 20) - "uv run python -m ebook_watchlist.web.build"
src/ebook_watchlist/web/static/

# Worktrees paralleler Agenten
.claude/worktrees/

# Bauergebnis des Design-Sync (ADR 20: nichts Gebautes ins Repo).
ds-bundle/
